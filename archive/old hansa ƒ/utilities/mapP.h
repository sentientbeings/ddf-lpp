/* this is the .h file for the private part of the map module			*/#pragma once#include <math.h>#include <QDOffscreen.h>#include "animation.h"#include "city.h"#include "cityInfoObject.h"#include "cityIndexObject.h"#include "cityNumberObject.h"#include "offer.h"#include "utilities.h"#include "view.h"#define mapID			128#define riverSegmentMatch(_a, _b, _c) (((_a) & (_b)) || ((_a) & (_c)))static CWindowPtr	mapWindow; /* used for the basic map */static CGrafPtr		currPort;static GWorldPtr	offScreenP;static GDHandle		currDev;static RgnHandle 	hexH;static Point drawCompass[12], hexLocation;void drawCoast(short row, short column);void drawHex(short row, short column);void drawRiver(short row, short column);void drawRiverSegment(short row, short column, enum direction dir);void InitColorPatterns(void);