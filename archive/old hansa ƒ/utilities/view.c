/* this contains the code for the view object type. It controls what	*//* kind of map is being drawn at the moment. Even though mapmaker never	*//* uses it directly, it is here because drawCity wants to know.			*/#include "view.h"#include "viewP.h"enum goodTypes GetCurrentView(){	return currentView;}enum viewMode GetViewMode(){	return currentViewMode;}void SetCurrentView(newGood)enum goodTypes newGood;{	currentView = newGood;}void SetViewMode(thisViewMode)enum viewMode thisViewMode;{	currentViewMode = thisViewMode;}