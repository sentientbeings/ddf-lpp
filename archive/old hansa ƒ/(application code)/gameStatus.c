/* this object keeps track of whether a game is currently happening. *//* It should be set to true anytime a new game is started. It should *//* be turned off anytime a game is finished. It initially starts off. */#include "gameStatus.h"#include "gameStatusP.h"Boolean IsGameInProgress(void){	return gameInProgress;}void SetGameInProgress(void){	gameInProgress = true;}void SetGameOver(void){	gameInProgress = false;}