/* This file contains the private include information for the main 		*//* module of Hansa. This is the static declarations private to the main	*//* module, and the prototypes for all private procedures.				*/#pragma once// #include <math.h>#include "animation.h"#include "changeMaps.h"#include "city.h"#include "display.h"#include "file.h"#include "leagueIcon.h"#include "map.h"#include "offer.h"#include "production.h"#include "trade.h"#include "utilities.h"#include "hansaWindows.h" /* resource IDs of menus */#define appleID			128#define fileID			129#define optionsID		130#define helpID			133/* options command indices */#define buildRailroadCommand	1#define sendSpiesCommand		2/* resource id number for the help alerts */#define offerAlertID		26792#define productionAlertID	5919#define startingUpAlertID	29793#define terrainAlertID		28780#define tradeAlertID		13395/* data local to this module */static int alertID[] = {	0, startingUpAlertID, productionAlertID, tradeAlertID, offerAlertID, 	terrainAlertID};static Rect dragRect = {0, 0, 1024, 1024};Boolean 			doMenuCommand(long mResult); enum mouseEventType	getTypeOfMouseEvent(Point where);void				initializeApplication(void);void 				main(void);Boolean 			ours(CWindowPtr w);Boolean				processAnEvent(void);void 				setUpMenus(void);