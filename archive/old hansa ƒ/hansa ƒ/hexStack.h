/* this is the public .h file for taking care of a stack of points */#pragma oncevoid	AdjustTransportCosts(void);short	CopyHexStack(Point stack[]);Point	GetLastHex(void);Point	GetTopHex(void);void	InitHexStack(void); /* set up an empty stack */void	InvertHexStack(void); /* inverts the list */void	InvertTopHex(void); /* go and flip the top hex on the screen */Boolean	PopHex(void); /* ditch the top element from the stack */Boolean PushHex(Point); /* add an element to the stack */short GetStackCost(void);short GetReverseCost(void);Boolean TrimHexStack(Point);