/* this is the .h file for the public part of the view module.			*/#pragma once#include "production.h"enum viewMode {	productionView, consumptionView, productionCostView, laborView, utilityView, normalView, numberOfViewModes, noView};enum goodTypes GetCurrentView(void);enum viewMode GetViewMode(void);void SetCurrentView(enum goodTypes newGood);void SetViewMode(enum viewMode thisViewMode);